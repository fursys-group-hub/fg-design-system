# design-qa — 자체 점검 (Foundation + Component)

- **대상 파일:** `시디즈_디자인 시스템` (`UsCx1wPybDpRRBglYY5Nmx`) — DS 정본 파일
- **모드:** 자체 점검 (기존 덤프 `sources/figma-raw.json` 재사용, 재호출 안 함)
- **검사 범위:** 페이지 `2. Foundation`, `3. Component`
- **덤프 lastModified:** 2026-08-12T07:05:46Z (재덤프 — 음수 gap 수정 반영)
- **검사 노드 수:** 1,754
- **정본 기준:** `index.md` → `tokens/{color,typography,spacing,effect}.md` + `components/*.md`
- **검사일:** 2026-08-12 (재검증)

> **재검증 결과: T4 명백 위반 0건.** 초기 점검의 유일한 명백 위반(Dropdown List `Select` 음수 gap `-1042px`, 노드 `66:2560`)이 피그마에서 수정됐고, 재덤프(`07:05:46Z`) 결과 `itemSpacing`이 제거(0)돼 Component 페이지 T4 위반 0건·음수 gap 잔존 0건을 확인했다. 나머지는 정본이 "구세대 잔존물"로 명시한 Foundation 견본과 `[확인 필요]` 항목(주의·정보 등급).

---

## 노이즈 제외 (오탐 — 위반 아님)

- **보라색 `#8A38F5`(190건)·`#9747FF`(12건):** 모든 COMPONENT_SET/Instance의 stroke와 placeholder로 검출됨 = **Figma 에디터의 컴포넌트 세트·인스턴스 표시 테두리 / 이미지 플레이스홀더 기본색**. 실제 UI 색이 아니므로 T2에서 제외. (CLAUDE.md의 "purple 금지"와 무관 — 렌더링 색이 아님)

---

## 위반 (명백) — 0건 ✅

| # | 페이지 | 컴포넌트 / 레이어 | 현재값 → 권장값 | 상태 | 근거 |
|---|---|---|---|---|---|
| 1 | 3. Component | Dropdown List / `Select` (`66:2560`) | `gap -1042px` → gap 제거(0) | **✅ 수정 완료** (재덤프 `07:05:46Z`에서 `itemSpacing` 없음 확인) | `tokens/spacing.md` |

## 주의 (정본에 없거나 근사 — 의도 확인 필요)

| # | 페이지 | 위치 | 현재값 → 권장값 | 건수 | 근거 |
|---|---|---|---|---|---|
| A | 3. Component | 텍스트(대부분 부모 `Label` = anatomy/스펙 라벨) | 스타일 `Body/Body4-12-Regular` → `Body/Body4-Regular`(정본) | 77 | `tokens/typography.md` |
| B | 3. Component | fills 다수 | `#222222` → Grey-800 `#242526` (Δ9, 근사) | 168 | `tokens/color.md` |
| C | 3. Component | fills | `#1D1D1B`·`#2D2E37`(검정계열, 팔레트 밖) → Grey-800/900 | 40 | `tokens/color.md` |
| D | 3. Component | fills | `#AAAAAA` → Grey-400 `#A4AAB0` (Δ12) / `#FEFEFE` → Grey-50 `#FFFFFF` | 8 | `tokens/color.md` |
| E | 3. Component | fills | `#F5CA1D`(노랑)·`#E7F6E7`·`#F9F4FF`·`#FCF7DF`·`#74778A` 등 팔레트 밖 | 8 | `tokens/color.md` |
| F | 3. Component | 텍스트 | 스타일 미적용 + 정본 외 크기(18·34px 등) 직접 지정 | 17 | `tokens/typography.md` |
| G | 3. Component | 텍스트 | 스타일 미적용(스타일 연결 없이 크기·굵기 직접 지정) | 17 | `tokens/typography.md` |
| H | 2. Foundation | 색·타이포 견본 프레임 | 구세대 스타일 `en/*`·`ko/*`·`Pretendard JP`(74) / 미적용 견본 텍스트(101) | 175 | `typography.md` §Foundation 안내표 (정본이 "잔존물 → 제외"로 명시) |
| I | 2. Foundation | 레이아웃 프레임(`Frame 100000…`) | gap 80·200·56 / padding 40 / radius 24 — 문서 레이아웃 여백(컴포넌트 토큰 스케일 밖) | 43 | `tokens/spacing.md` (Foundation 레이아웃은 토큰 집계 범위 밖) |

## 정보 (네이밍)

| # | 페이지 | 위치 | 내용 | 건수 | 근거 |
|---|---|---|---|---|---|
| J | 3. Component | Input(28)·Table Cell(13)·Button(12)·Dropdown List(5)·Sidebar(4)·Tab(2)·Carousel(2)·Input Case(2) | variant 속성명 오타 `Varient` → `Variant` | 68 | 네이밍 규칙 (정본 `index.md`에도 `Varient`로 반영됨 — 파일·문서 동시 수정 필요) |
| K | 2·3 | 다수 | 레이어명 기본값 방치 `Frame 100000…`·`Rectangle N` 등 | 265 | 네이밍 규칙 |

---

## `[확인 필요]`

- **A·B의 실사용 여부:** `Body/Body4-12-Regular`(77)와 `#222222`(168) 상당수가 부모 `Label`/anatomy 프레임 소속으로 보여, **실 컴포넌트 slot인지 문서용 스펙 라벨인지 REST JSON만으로 확정 불가**. 실 컴포넌트 사용이면 위반, 스펙 라벨이면 정리 권장 수준.
- **T3 컴포넌트 준수:** 자체 점검(정의 페이지)이라 INSTANCE 오버라이드/직접 그린 유사요소 판별 대상이 적음 — 별도 위반 미검출, 실무 파일 점검 시 재확인 대상.
- **T4 변수 토큰명:** spacing은 Figma 변수 바인딩이라 토큰명이 REST 미제공 → 값 기준 대조. 바인딩된 값은 위반에서 제외함.

---

## 항목별 집계 (정제 후)

| 항목 | 위반 | 주의 | 정보 |
|---|---|---|---|
| T1 타이포 | 0 | 111 (Component) + 175 (Foundation 견본) | — |
| T2 컬러 | 0 | 224 (핵심: `#222222` 168) | — |
| T3 컴포넌트 | 0 | `[확인 필요]` | — |
| T4 스페이싱 | **0** ✅ (음수 gap 수정 완료) | 43 (Foundation 레이아웃) | — |
| T5 네이밍 | 0 | — | 333 (`Varient` 68 + 기본 레이어명 265) |

**clean 여부:** ❌ 아님 (명백 위반 0 · 주의 다수 · 정보 333). **T4는 clean.**
**남은 조치:** ① `Varient`→`Variant` 8개 컴포넌트 일괄 + `index.md` 동기 → ② `Body4-12-Regular`·`#222222` 실사용 여부 확인 후 정본 스타일/토큰으로 교체.
