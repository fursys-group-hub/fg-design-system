# (폐기) snippets — COMPONENTS.html 로 대체됨

이 폴더의 조각 18종은 **더 이상 정본이 아니다.** 2026-08-21 v5 개정으로
마크업 정본이 `design-system/COMPONENTS.html`(+ `fg-components.css`)로 일원화되었다.

- 화면 생성 시 여기 조각을 쓰지 말고 `COMPONENTS.html` 의 마크업을 그대로 복사해 조립한다.
- dist/fg/ 로 복사하지 않는다.
- 이력 참고용으로만 보관한다.
