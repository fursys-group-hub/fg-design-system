# Spacing / Radius — 퍼시스그룹 간격·코너 토큰

원본: Figma `퍼시스그룹_디자인 시스템` (`UsCx1wPybDpRRBglYY5Nmx`), `3. Component` 페이지의 변수 바인딩 값 집계.
간격·radius는 **피그마 변수로 등록되어 있지 않다**(피그마 로컬 발행 변수는 색뿐 — Fursys Group 22개 · Secondary Colors 65개). 아래는 컴포넌트 노드에서 집계한 실사용 값 스케일이며, 코드 명칭 `--fg-space-{n}` · `--fg-radius-{n}` 이 기준이다(괄호=사용 빈도). 자세한 근거는 아래 「변수 토큰명 (확정)」 절 참조.

## Corner Radius

*용도:* 버튼/인풋 등 기본 4·6px, 큰 곡률 32·40px, `9999`=pill(태그/칩/원형).

`2`(39) · `4`(77) · `5`(13) · `6`(78) · `32`(17) · `40`(17) · `9999`(29)

## Padding

*8·12px가 주력.* 큰 값(64·80)은 섹션/레이아웃 여백. (`10`은 Button Flat 등 소수 컴포넌트에서 사용)

`2`(16) · `4`(68) · `5`(1) · `6`(32) · `8`(226) · `10`(8) · `12`(289) · `16`(37) · `20`(40) · `24`(6) · `32`(6) · `64`(68) · `80`(68)

## Gap (itemSpacing)

`2`(13) · `4`(15) · `6`(36) · `8`(52) · `10`(18) · `12`(71) · `16`(19) · `20`(7) · `24`(22) · `40`(17) · `64`(17) · `100`(3)

## 변수 토큰명 (확정)

스페이싱과 Radius 변수는 피그마에 등록되어 있지 않다. 피그마의 로컬 발행 변수는 색뿐이다(Fursys Group 22개 · Secondary Colors 65개). 따라서 대조할 원장 명칭이 없고, 코드 명칭 `--fg-space-{n}` · `--fg-radius-{n}` 이 기준이다. 값은 위 스케일과 일치한다. (2026-09-04 내보낸 피그마 파일 기준으로 확인)

> 피그마에 `Gap 2` · `spacing/4` · `radius/rounded-lg` 같은 숫자 변수가 있지만 전부 외부 라이브러리에서 들어온 것이고 스케일도 정본과 다르다(48이 있고 5·10·64·80이 없다). 기준으로 쓰지 않는다.

## 확인 필요

- 4·8 그리드 외 값(2·5·6·10px 등)이 의도된 토큰인지, 예외인지.
